version: 2.1

jobs:
  deploy:
    docker:
      - image: cimg/python:3.12
    steps:
      - checkout

      - run:
          name: Install system dependencies for dlib
          command: |
            sudo apt-get update
            sudo apt-get install -y \
              build-essential \
              cmake \
              g++ \
              python3-dev \
              libopenblas-dev \
              liblapack-dev \
              libatlas-base-dev \
              libx11-dev \
              libjpeg-dev \
              libpng-dev \
              libfreetype6-dev \
              zlib1g-dev \
              libboost-all-dev

      - run:
          name: Install Python dependencies
          command: |
            python -m pip install --upgrade pip
            pip install -r requirements.txt
            pip install awsebcli

      - run:
          name: Deploy to Elastic Beanstalk
          command: |
            eb init crm-app --platform python-3.12 --region $AWS_DEFAULT_REGION
            eb use crm-env
            eb deploy

workflows:
  deploy-workflow:
    jobs:
      - deploy
